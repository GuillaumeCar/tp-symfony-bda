{% extends 'base.html.twig' %}

{% block title %}Hello DatalistController!{% endblock %}

{% block body %}
<div>
    {% for post in posts %}
    <div class="row">
        <h3>{{ post.title }}</h3>
        <p>{{ post.description }}</p>
    </div>
    {% endfor %}
</div>
{% endblock %}
