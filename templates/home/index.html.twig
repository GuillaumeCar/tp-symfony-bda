{% extends 'base.html.twig' %}

{% block body %}
    {% for post in posts %}
        <div class="row">
            <h3>{{ post.title }}</h3>
            <p>{{ post.description }}</p>
        </div>
    {% endfor %}
{% endblock %}